name: TS Format

on:
  push:
    branches: [master]
    paths:
      - "**/*.ts"
      - "**/*.json"
      - "**/*.md"
      - ".prettierrc.json"
      - ".prettierignore"
      - ".github/workflows/ts-format.yml"
  pull_request:
    branches: [master]
    paths:
      - "**/*.ts"
      - "**/*.json"
      - "**/*.md"
      - ".prettierrc.json"
      - ".prettierignore"
      - ".github/workflows/ts-format.yml"

concurrency:
  group: ts-format-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  format:
    runs-on: parity-int-ubuntu-xl
    steps:
      - uses: actions/checkout@v4
      # `oven-sh/setup-bun@v2` unzips its tarball at install time and needs
      # `unzip` on PATH, which parity-int-* images do not ship by default.
      - name: Install unzip
        run: sudo apt-get update && sudo apt-get install -y unzip
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.2.6"
      - run: bun install --frozen-lockfile --ignore-scripts
      - run: bun run format
